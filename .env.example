# ── Ollama connection ─────────────────────────────
OLLAMA_HOST=localhost
OLLAMA_PORT=11434

# ── Models ───────────────────────────────────────
# GPU machine  → llama3  (better quality, ~5-10s)
# CPU only     → phi3:mini  (faster, ~15-30s)
OLLAMA_MODEL=llama3:latest
EMBED_MODEL=nomic-embed-text
